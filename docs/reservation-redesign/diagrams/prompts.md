# 생성 프롬프트 기록

도구: 내장 image_gen. 원본 첨부 이미지를 참조하여 생성 후 검토·수정.

## 1 최초 생성

Edit target: attached architecture sketch. Redraw as clean professional Korean technical diagram, page 1 of consistent four-page series. Landscape 1800x1200 or higher, white background, precise rounded rectangles, thin navy lines, large perfectly legible Korean sans-serif typography. Blue reservation A, teal reservation B, amber atomic boundary. No handwritten text, no tiny JSON paragraphs. Title "01 예약 생성 · 비중첩 구간". Subtitle "운행 10898 · 좌석 16664 · 요청 10:20 · 유효기간 10분 예시". Top route Seoul 서울 (0) → 대전 (1) → 동대구 (2) → 부산 (3); below segments "구간 0 [0,1)" "구간 1 [1,2)" "구간 2 [2,3)". Two horizontal lanes sequential state snapshots. Lane A request card "예약 A / res_A / 서울 → 동대구 [0,2)" → "검사 구간 0, 1 / 모두 비어 있음" → four evenly aligned small data cards inside a single dotted amber boundary "Redis · 전체 검증 후 원자적 저장": occupancy HASH entries "16664:0 → res_A" "16664:1 → res_A"; reservations HASH "res_A: HELD / expiresAt 10:30"; deadlines ZSET "res_A → 10:30"; idempotency HASH "회원A:req_A → res_A". Right success pill "201 성공". Lane B request "예약 B / res_B / 동대구 → 부산 [2,3)" → "검사 구간 2 / 비어 있음" → four cards boundary: occupancy HASH all three fields 16664:0 → res_A;16664:1 → res_A;16664:2 → res_B (new field teal); reservations HASH res_A: HELD;res_B: HELD; deadlines ZSET res_A → 10:30;res_B → 10:30; idempotency HASH 회원A:req_A → res_A;회원B:req_B → res_B. success201. Draw arrows from validation to group not misleading sequential DB chains. Footer "같은 좌석도 구간이 겹치지 않으면 예약 가능" and small "공통 키 접두사 rail:{schedule:10898}: · 시각은 설명용, 실제 저장은 UTC ms". Preserve original example concept but correct expiresAt and schema occupancy. Need ample spacing readable at full size.

## 2 최초 생성

Use attached original as subject reference but create a clean professional Korean technical diagram page2, consistent white background, navy rounded rectangles, blue A, teal B, red conflict, large Korean sans typography, landscape1800x1200 or higher. Title "02 좌석 충돌 · 요청 전체 거부". Top route 서울 (0) → 대전 (1) → 동대구 (2) → 부산 (3); segments0 blue res_A,1 blue res_A,2 teal res_B. Subtitle "운행 10898 · 좌석 16664 · A/B 예약이 유효한 상태". Main horizontal flow: card "예약 C / 대전 → 동대구 [1,2) / 좌석 16664" → card "검사 대상 / 16664:1" → Redis occupancy lookup card "16664:1 → res_A" → owner reservations card "res_A / HELD / now < expiresAt" → red stop card "409 좌석 충돌". Atomic dashed boundary around lookup and validation. Below large panel "Redis 상태 유지 · C는 저장하지 않음", four columns occupancy HASH (16664:0 → res_A;16664:1 → res_A;16664:2 → res_B), reservations HASH res_A, res_B; deadlines ZSET res_A→10:30 res_B→10:30; idempotency HASH 회원A:req_A→res_A 회원B:req_B→res_B. Dashed red arrow from conflict to state panel annotated "추가·변경 없음". Bottom prominent note "대전 → 동대구 [1,2)는 구간 1만 검사". Another small scenario strip "여러 좌석 중 하나라도 충돌 → 빈 좌석도 저장하지 않음". Footer "HELD 만료 전·CONFIRMING·CONFIRMED 점유는 충돌". Do not show res_C in stored hash or deadlines. No unnecessary request JSON.

## 3 최초 생성

Use attached original as diagram subject/style inspiration but make polished Korean technical diagram page3 landscape1800x1200 or higher, white background, navy outlines, blue Redis pale fill, purple DB pale fill, amber transactional boundaries, large readable Korean sans. Title "03 결제 준비 · Order 생성". Top small label "설계안 · 단일 예약 res_A 예시". Subtitle "10:22 결제 준비 요청 · 원래 만료 10:30". Flow with clear numbered cards in two rows, no crossed arrows. 1 사용자 "res_A로 결제 준비" arrow 2 DB "준비 기록 확보 / 준비 멱등성 키 / order_1 발급" with small DB TX1 label. Arrow3 Redis atomic boundary card "예약 검증 + 주문 연결": "회원·HELD·만료 전 확인 / 모든 구간의 소유자 확인 / orderId = order_1". Arrow4 DB TX2 boundary "Order·결제 정보 저장": Order(PENDING), OrderBooking(예약 스냅샷), OrderSeatBooking(좌석·운임), Payment(PENDING), 준비 기록 완료. Arrow5 user response "orderId · amount · expiresAt / 결제창 준비". Beneath middle create before/after card: "Redis 연결 후" reservations HASH res_A {status: HELD, orderId: order_1, expiresAt:10:30}; occupancy HASH16664:0→res_A and16664:1→res_A "점유 유지"; deadlines ZSET res_A→10:30 "기한 유지". Clear emphasis "결제 준비에서는 CONFIRMING으로 바꾸지 않음". Bottom restrained failure branch from DB TX2: "DB 저장 실패가 확실함 → 같은 주문 연결만 해제"; second text "커밋 결과 불명 → DB 결과 확인 후 판단". Header caption "DB와 Redis는 별도 처리 · 하나의 공통 트랜잭션이 아님". Do not show provider call, ticket or booking creation in prepare. No Redis Stream. Legibility over density.

## 4 최초 생성

Create page4 polished Korean technical architecture diagram using original as conceptual reference, same white background navy lines rounded rectangles, blue Redis, purple DB, green success, amber boundaries; landscape1800x1350 or higher large legible Korean sans text. Title "04 결제 승인 → 예매 확정". Small badge "설계안 · order_1 / res_A". Main numbered flow 6 steps, upper row left to right 1 "승인 요청 검증 / 주문·금액 확인 / DB 승인 시도 기록" → 2 Redis atomic box "승인 진입 / HELD → CONFIRMING / deadlines에서 제거 / confirmation-deadlines에 등록 / 좌석 점유 유지" →3 external card "결제사 승인 요청 / DB 트랜잭션 밖에서 호출". Down from3 to second row step4 DB large purple transaction box "승인 성공 · DB 단일 커밋" contains Payment→PAID, Order→ORDERED, Booking 생성, SeatBooking 생성, Ticket 발급, BookingConfirmed Outbox 저장. Arrange row2 left to right by arrows with 3 via vertical to4 located lower left using perimeter connector avoid crossing. step4 →5 "커밋 후 Redis 반영 / 즉시 실행 또는 Outbox Worker" →6 Redis box "CONFIRMING → CONFIRMED / confirmation-deadlines에서 제거 / occupancy 유지". Make after state small card below6 "16664:0 → res_A / 16664:1 → res_A / res_A: CONFIRMED" green. Branch from DBcommit to green user success "예매 완료 · 승차권 조회". Annotation "완료 응답 기준: 승인 성공 + DB 커밋". Bottom thin red/amber failure strip with 3 separate boxes: "승인 실패 확정 → 조건부 점유 해제"; "타임아웃·결과 불명 → CONFIRMING 유지 · 결제 결과 조회"; "DB 커밋 후 Redis 반영 실패 → Outbox Worker 재시도". Prominent footer "결제 완료 후에도 좌석 점유를 삭제하지 않음". No Redis Stream; Outbox is DB table. Clear distinction database transaction vs Redis atomic operation. Preserve readable numbered sequential order no vague cyclic arrows.

## 최종 수정 지시

- 01: 두 seat_occupancy 제목을 실제 키 이름 occupancy로 변경. 나머지 배치 유지.
- 02: seat_occupancy를 occupancy로 변경하고 마지막 설명을 동일 구간 충돌로 한정.
- 03: 입력을 예약 참조·준비 멱등성 키로 수정. occupancy로 통일. 원래 만료 시각 유지 및 이미 만료된 경우 처리 명시. 미확정 준비 상태 enum 제거. 금액은 예약 금액으로 표시. Redis 롤백 대신 조건부 주문 연결 해제로 명시.
- 04: 승인 대기 TTL을 결과 재확인 시각·ZSET으로 수정. 미확정 승인 시도 enum 제거. PG 호출 전 진입 성공·전송 단계 DB 기록 표시. 성공 분기만 DB 확정으로 연결. 미요청 이메일 항목 제거. 실패 분기는 DB 실패 기록·Outbox 저장으로 정정.

